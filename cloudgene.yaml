id: dummy-cloudgene-app
name: Dummy Cloudgene App
description: Dummy app for testing purposes.
version: 0.1.0
category: Application
submitButton: Run

workflow:
  steps:
    - name: Hello from the Nextflow
      type: nextflow
      script: main.nf
      groups:
        - id: main
          label: Main
      processes:
        - process: "nextflowStep"
          view: label
          group: main

  inputs:
    -id: dummy-label
    type: label
    description: This dummy takes no input from anybody.
